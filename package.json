{
  "name": "typocitos-game",
  "version": "1.0.0",
  "scripts": {
    "build:docs": "cd ../typocitos/api && npm i && npm run-script docs",
    "build:game": "cd ../typocitos && npm i && npm run-script lint && npm run-script build",
    "dist:mv": "move ../typocitos/dist ./dist",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "npm-run-all": "^4.1.5"
  }
}